# requirements.txt — Python dependency manifest for hao-backprop-test
# All versions are verified against PyPI for Python 3.12 compatibility (AAP §0.5.2).
# Use exact pins (==) for reproducible builds.
#
# Every pin below is exactly the AAP §0.5.2 value. The AAP explicitly pins
# these versions "for reproducibility"; loosening or substituting any pin
# would break the byte-for-byte parity guarantee the refactor was developed
# against and would diverge from the canonical project descriptor in
# pyproject.toml. Keep this file and pyproject.toml in lock-step.

# --- Web framework -----------------------------------------------------------
Flask==3.1.3
# Werkzeug 3.1.3 — Flask's WSGI utility library; pinned to the AAP §0.5.2
# version for reproducibility. Flask 3.1.3 would resolve this transitively,
# but pinning explicitly ensures the lock-step manifest matches AAP §0.5.2.
Werkzeug==3.1.3

# --- Environment configuration ----------------------------------------------
# python-dotenv 1.0.1 — loads .env into os.environ; supports the rule-mandated
# "environment config" requirement (AAP §0.7.3). Pinned exactly per AAP §0.5.2.
python-dotenv==1.0.1

# --- Production WSGI servers -------------------------------------------------
# gunicorn is the primary POSIX server (Linux/macOS); it does NOT support Windows.
gunicorn==23.0.0 ; sys_platform != "win32"
# waitress is the cross-platform fallback used on Windows or when gunicorn is unavailable.
waitress==3.0.2

# --- Testing -----------------------------------------------------------------
# pytest 8.4.2 — pinned exactly per AAP §0.5.2 for the parity test suite.
# Verified compatible with pytest-flask 1.3.0, which declares `pytest >=5.2`
# as its minimum supported version.
pytest==8.4.2
pytest-flask==1.3.0
